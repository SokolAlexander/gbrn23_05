import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    borderColor: '#333333',
    borderWidth: 2,
    borderRadius: 4,
    paddingHorizontal: 20,
    fontSize: 18,
  },
});
